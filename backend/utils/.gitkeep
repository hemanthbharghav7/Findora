/**
 * utils/
 * ------
 * Shared utility / helper functions for the Findora backend.
 *
 * Files to be created here (future):
 *  - generateToken.js   – Sign and return a JWT for a given user ID
 *                         Usage: const token = generateToken(user._id);
 *
 *  - asyncHandler.js    – Wrap async route handlers to avoid try/catch boilerplate.
 *                         Usage: router.get('/', asyncHandler(async (req, res) => { ... }));
 *
 *  - apiFeatures.js     – Class that chains .filter(), .sort(), .paginate()
 *                         methods on a Mongoose query for the GET /api/items endpoint.
 */

// This file is intentionally left empty.
// It serves as a placeholder so Git tracks the utils/ directory.
